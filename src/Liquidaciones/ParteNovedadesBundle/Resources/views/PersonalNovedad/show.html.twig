<h1>PersonalNovedad</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fechadesde</th>
            <td>{{ entity.FechaDesde|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fechahasta</th>
            <td>{{ entity.FechaHasta|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Cantidadminutospordia</th>
            <td>{{ entity.CantidadMinutosPorDia }}</td>
        </tr>
        <tr>
            <th>Observaciones</th>
            <td>{{ entity.Observaciones }}</td>
        </tr>
        <tr>
            <th>Eliminado</th>
            <td>{{ entity.Eliminado }}</td>
        </tr>
        <tr>
            <th>Fecha_modi</th>
            <td>{{ entity.FechaModi|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Usua_modi</th>
            <td>{{ entity.UsuaModi }}</td>
        </tr>
        <tr>
            <th>Depe_modi</th>
            <td>{{ entity.DepeModi }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('personalnovedad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('personalnovedad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('personalnovedad_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
