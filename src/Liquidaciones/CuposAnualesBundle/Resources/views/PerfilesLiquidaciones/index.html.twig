

{% block body -%}
    <h1>PerfilesLiquidaciones list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Perfil</th>
                <th>Activo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('perfilesliquidaciones_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.perfil }}</td>
                <td>{{ entity.activo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('perfilesliquidaciones_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('perfilesliquidaciones_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('perfilesliquidaciones_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
