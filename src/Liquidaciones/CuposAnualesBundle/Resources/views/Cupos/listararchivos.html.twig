{% extends '::base.html.twig' %}

{% block guia %}
    {{ parent() }}
    <li><a href="{{ path('cuposanuales') }}">Cupos Mensuales</a></li>
    <li class="active">Archivos Cargados</li>
{% endblock %}
    
{% block contenido %}
    <style>
        .form-control, .btn-ba {
            margin-bottom: 10px;
        }
        
        .form-control, .btn-success, .btn-default {
            margin-bottom: 10px;
        }
        
    </style>
    <h1>Archivos</h1>
    
    {% if app.session.flashbag.has('aviso_error') %}
        <div class="alert alert-danger alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert">
                <span aria-hidden="true">&times;</span>
                <span class="sr-only">Close</span>
            </button>
        {% for flashMessage in app.session.flashbag.get('aviso_error') %}
            <p>
                {{ flashMessage }}
            </p>
        {% endfor %}
        </div>
    {% endif %}
    
    
    
    
    
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Archivos Cargado Marzo 2020</h3>
        </div>
        <div class="panel-body">
           <table class="table table-striped table-responsive table-bordered" id="example2">
            <thead>
                <th>Año</th>
                <th>Mes</th>
                <th>Dependencia</th>
                <th>Archivo</th>
            </thead>
            <tbody>
                {% for comment in tabla %}
                <tr>
                    <td>{{comment.anio}} </td>
                    <td>{{comment.mes}} </td>
                    <td>{{comment.depe}} </td>
                    <td><button class="btn btn-primary" style="margin-right: 10px;"><a style="color:white !important;" href="{{ asset('uploads/planillas/' ~ comment.carpeta ~ '/' ~ comment.archivo) }}" target="_blank">DESCARGAR</a></button></td>
                </tr>
                {% endfor %}
            </tbody>
           </table>
        </div>
    </div>
    
    <script>
        
        $(document).ready(function(){
           

            
        });
        
    </script>
{% endblock %}
