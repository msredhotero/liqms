{% extends '::base.html.twig' %}

{% block contenido -%}
    <h1>TipoDependencia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codtipodependencia</th>
                <th>Desctipodependencia</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipodependencia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codTipoDependencia }}</td>
                <td>{{ entity.descTipoDependencia }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tipodependencia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipodependencia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipodependencia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
