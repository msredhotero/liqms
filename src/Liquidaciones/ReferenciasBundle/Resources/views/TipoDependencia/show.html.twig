{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoDependencia</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codtipodependencia</th>
                <td>{{ entity.codTipoDependencia }}</td>
            </tr>
            <tr>
                <th>Desctipodependencia</th>
                <td>{{ entity.descTipoDependencia }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipodependencia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipodependencia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
